# Proyecto1SO
Simulacion de Atencion de Procesos con Pthreads
Compilar con:
``` gcc <nombre_archivo>.c -o <nombre_ejecutable> -lpthread -lm```

Ejecucion con
``` ./server```
``` ./client```
``` ./client file.txt```